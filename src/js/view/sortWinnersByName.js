import getData from "../app/getData";
const nameBtn = document.querySelector(".subheader__item");

nameBtn?.addEventListener("click", function () {
	if()
	getData("http://127.0.0.1:3000/winners?_sort=id&_order=", sortMethod)
});
